# Janus daemon defaults
JANUS_PROXY_BIND=127.0.0.1:9080
JANUS_CONTROL_SOCKET=/tmp/janusd-control.sock
# Optional read-only discovery API for jailed MCP (do not expose publicly without network policy)
# JANUS_DISCOVERY_BIND=127.0.0.1:9181
JANUS_DEFAULT_TTL_SECONDS=3600

# Host allowlist for proxy/tunnels
JANUS_ALLOWED_HOSTS=github.com,api.github.com,codeberg.org,api.cohere.ai,postgres.internal,mysql.internal,redis.internal,mongodb.internal,rabbitmq.internal,kafka.internal,nats.internal,mqtt.internal,ldap.internal,sftp.internal,smb.internal
JANUS_GIT_HTTP_HOSTS=github.com,codeberg.org

# Default session capabilities.
# This example enables all currently supported protocols.
JANUS_DEFAULT_CAPABILITIES=http_proxy,git_http,git_ssh,postgres_wire,mysql_wire,redis,mongodb,amqp,kafka,nats,mqtt,ldap,sftp,smb

# Protocol mapping reference:
# - git_ssh,sftp -> port 22
# - postgres_wire -> 5432
# - mysql_wire -> 3306
# - redis -> 6379
# - mongodb -> 27017
# - amqp -> 5672
# - kafka -> 9092
# - nats -> 4222
# - mqtt -> 1883/8883
# - ldap -> 389/636
# - smb -> 445
# Non-HTTP protocols are controlled by capability + JANUS_ALLOWED_HOSTS.

# Git HTTP auth
# JANUS_GIT_HTTP_USERNAME=x-access-token
# JANUS_GIT_HTTP_PASSWORD=replace-me

# Git SSH auth (host ssh-agent socket)
# JANUS_GIT_SSH_AUTH_SOCK=/var/run/janus/ssh-agent.sock
# JANUS_GIT_SSH_PRIVATE_KEY_FILE=/run/secrets/janus_git_ssh_key

# JANUS_KUBECONFIG=/home/user/.kube/config

# UI
# JANUS_NO_BANNER=1
