# Evolution Event: proxy pages

## Timestamp
2026-03-26T00:00:00+00:00

## Trigger
User added a static website for the proxy and asked whether it can be deployed as GitHub Pages.

## Change
Added a dedicated `proxy-pages` GitHub Actions workflow that stages the static files from `site/` and deploys them with GitHub Pages.
Follow-up cleanup ignored local tool-state directories in the repo root and clarified that npm page content comes from the published package README rather than the website files under `site/`.

## Decision Link
- ADR:
- Task decision: [TASK-PROXY-PAGES-proxy-pages.md](../../docs/decisions/TASK-PROXY-PAGES-proxy-pages.md)

## Validation Evidence
- `.github/workflows/proxy-pages.yml`
- `site/index.html`
- `python3 -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/proxy-pages.yml').read_text())"`

## Outcome
Success

## Follow-up
- Enable GitHub Pages for the repository if it is not already configured to deploy from GitHub Actions.
