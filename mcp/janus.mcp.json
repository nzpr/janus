{
  "mcpServers": {
    "janus": {
      "command": "bun",
      "args": [
        "run",
        "/ABS/PATH/TO/janus/src/mcp-server.ts",
        "--workspace",
        "/ABS/PATH/TO/workspace",
        "--client",
        "host"
      ]
    }
  }
}
